import { Routes } from '@angular/router';
import { ScenarioSessionComponent } from './scenario-session/scenario-session.component';

export const SCENARIOS_ROUTES: Routes = [{ path: '', component: ScenarioSessionComponent }];
